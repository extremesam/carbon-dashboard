# carbon-dashboard
an interactive dashboard where a user can input a CSV of facility addresses. Use an open-source mapping library (like Leaflet or MapLibre) to display these locations on a map, styled by their estimated carbon risk.
